import { Meta } from '@storybook/addon-docs';

<Meta title="Installation" />

<h1 style={{fontWeight: 600, textAlign: 'center'}}> React Filter Editor 🔎</h1>
<div style={{marginBottom: "2rem"}}></div>

## Getting Started 🚀 

### Install Filter Editor

```sh
npm install react-filter-editor
```

### Import component 🔎

```jsx
import React, { useState } from 'react';
import { FilterEditor } from 'react-filter-editor';

const options = [
  { name:"name", label:"Name Person", fieldType: "text" },
  { name:"email", label:"Email", fieldType: "text", icon: "fas fa-envelope" },
  { name:"age", label:"Age", fieldType: "number" },
];

const defaultValues = {
  name: "Juan",
  email: "jpprogrammer1@gmail.com",
  age: "21"
}

const App = () => {
  const [values, setValues] = useState({...defaultValues});
  return (
    <FilterEditor options={options} values={values} onChangeValues={setValues}/>
  );
};
```
#### Import component for Bootstrap v5
```jsx
import FilterEditor from 'react-filter-editor/lib/bootstrap';
```