import { Meta } from '@storybook/addon-docs';

<Meta title="Props/className" />

## className
is inserted as the parent class of the filter container

```jsx
  <FilterEditor options={options} values={values} onChangeValues={setValues} className="container-filters" />
```